import { Highlightv1Directive } from './highlightv1.directive';

describe('Highlightv1Directive', () => {
  it('should create an instance', () => {
    const directive = new Highlightv1Directive();
    expect(directive).toBeTruthy();
  });
});
