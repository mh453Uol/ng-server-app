import { Highlightv2Directive } from './highlightv2.directive';

describe('Highlightv2Directive', () => {
  it('should create an instance', () => {
    const directive = new Highlightv2Directive();
    expect(directive).toBeTruthy();
  });
});
