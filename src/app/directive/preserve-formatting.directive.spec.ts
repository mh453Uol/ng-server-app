import { PreserveFormattingDirective } from './preserve-formatting.directive';

describe('PreserveFormattingDirective', () => {
  it('should create an instance', () => {
    const directive = new PreserveFormattingDirective();
    expect(directive).toBeTruthy();
  });
});
